console.log(JSON.stringify({
  ok: true,
  mode: 'locked_local_account_canary',
  canary: 'LOCKED',
  reason: 'Account Core canary requires explicit owner approval after dry-run and verify.',
  productionChanged: false,
}, null, 2));
